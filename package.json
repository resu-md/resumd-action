{
  "name": "resumd-action",
  "version": "0.1.0",
  "description": "GitHub Action for building resume PDFs and HTML from Markdown and CSS.",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/glob": "^0.4.0",
    "gray-matter": "^4.0.3",
    "markdown-it": "^13.0.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
